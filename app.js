let BRAND_NAME = 'I am a hero'
let BIG_BRAND_NAME = BRAND_NAME.toUpperCase();

console.log( ` Original = ${BRAND_NAME} \n Modified =${BIG_BRAND_NAME}`);
alert( ` Original = ${BRAND_NAME} \n Modified =${BIG_BRAND_NAME}`);
